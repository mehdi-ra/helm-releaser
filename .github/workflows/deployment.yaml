on:
  push:
    branches:
      - main

jobs:
  test-runenr:
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v3

      - name: Helm release test
        uses: devopshobbies/helm-releaser@v1
        with:
          token: ""
          genericChart: ""
          kubeConfig: ""
